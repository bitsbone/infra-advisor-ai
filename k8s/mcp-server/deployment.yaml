apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server
  namespace: infra-advisor
  labels:
    app: mcp-server
    tags.datadoghq.com/env: dev
    tags.datadoghq.com/service: infra-advisor-mcp-server
    tags.datadoghq.com/version: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mcp-server
  template:
    metadata:
      labels:
        app: mcp-server
        tags.datadoghq.com/env: dev
        tags.datadoghq.com/service: infra-advisor-mcp-server
        tags.datadoghq.com/version: latest
      annotations:
        ad.datadoghq.com/mcp-server.logs: '[{"source":"python","service":"infra-advisor-mcp-server"}]'
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: mcp-server
          image: ghcr.io/bitsbone/infra-advisor-ai/mcp-server:latest
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - configMapRef:
                name: mcp-server-config
            - secretRef:
                name: mcp-server-secret
          env:
            # Explicit env values take precedence over envFrom, including any
            # legacy secret that enabled automatic content capture.
            - name: DD_LLMOBS_ENABLED
              value: "false"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /livez
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 60
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /livez
              port: 8000
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 24
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
